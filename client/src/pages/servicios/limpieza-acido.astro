---
import Layout from "../../layouts/Layout.astro";
import limpiezaAcidoAntes from "@/assets/limpiezaAcidoAntes.webp";
import limpiezaAcidoDespues from "@/assets/limpiezaAcidoDespues.webp";
import {
  CircleCheckIcon,
  SparklesIcon,
  ShieldCheckIcon,
  MoveLeftIcon,
} from "@lucide/astro";
import CarouselGallery from "@/components/CarouselGallery";
import ServiceCTA from "@/components/ServiceCTA.astro";

const service = {
  title: "Limpieza con ácido",
  desc: "Tu pileta como nueva: limpieza ácida + hidrolavado profesional, con equipo propio y protocolos de seguridad.",
  alt: "Limpieza con ácido en pileta",
  slug: "limpieza-acido",
};

const WSP = "5492644822296";
const wspHref = `https://wa.me/${WSP}?text=${encodeURIComponent(
  `Hola! Quiero solicitar el servicio de ${service.title}`
)}`;

const slides = [
  { src: limpiezaAcidoAntes, alt: service.alt + " (antes)" },
  { src: limpiezaAcidoDespues, alt: service.alt + " (después)" },
];
---

<Layout title={service.title} description={service.desc}>
  <article class="space-y-10 mx-auto px-6 py-12 container">
    <!-- Top bar -->
    <nav class="flex justify-start items-center mb-6">
      <a
        href="/"
        aria-label="Volver al inicio"
        class="inline-flex items-center gap-2 bg-neutral-100 hover:bg-neutral-200 px-4 py-2 rounded-full font-medium text-neutral-800 transition-colors"
      >
        <MoveLeftIcon class="w-5 h-5" />
        <span>Inicio</span>
      </a>
    </nav>

    <!-- Header -->
    <header class="space-y-2">
      <h1
        class="font-bold text-neutral-900 text-3xl md:text-4xl tracking-tight"
      >
        {service.title}
      </h1>
      <p class="text-neutral-700 text-lg">{service.desc}</p>
      <p class="font-medium text-blue-700/90">
        💥 Resultado: superficies impecables, listas para llenar o pintar.
      </p>
    </header>

    <!-- Cover -->
    <section class="relative">
      <CarouselGallery slides={slides} client:load />
    </section>

    <!-- Cards -->
    <section class="gap-6 grid grid-cols-1 md:grid-cols-3">
      <!-- ¿Qué incluye? -->
      <div class="bg-white shadow-sm p-6 border border-neutral-200 rounded-2xl">
        <div class="inline-flex items-center gap-2 mb-3">
          <CircleCheckIcon class="w-5 h-5 text-emerald-600" />
          <h2 class="font-semibold text-neutral-900 text-xl">¿Qué hacemos?</h2>
        </div>
        <ul class="space-y-2 mt-4 pl-6 text-neutral-700 list-disc">
          <li>Desagote completo con bombas propias.</li>
          <li>
            Limpieza con ácido controlada (paredes, piso y línea de agua).
          </li>
          <li>
            <strong>Hidrolavado</strong> para despegar sarro y suciedad adherida.
          </li>
          <li>
            Enjuague y <strong>neutralizado</strong> para cuidar superficies y solados.
          </li>
          <li>Todo el equipo es nuestro. No usamos elementos del hogar.</li>
        </ul>
      </div>

      <!-- Beneficios -->
      <div class="bg-white shadow-sm p-6 border border-neutral-200 rounded-2xl">
        <div class="inline-flex items-center gap-2 mb-3">
          <SparklesIcon class="w-5 h-5 text-indigo-600" />
          <h2 class="font-semibold text-neutral-900 text-xl">Beneficios</h2>
        </div>

        <ul class="space-y-2 mt-4 pl-6 text-neutral-700 list-disc">
          <li>Color y brillo original. Se va el sarro, algas y manchas.</li>
          <li>Olor neutralizado y superficies seguras al tacto.</li>
          <li>Mejor adherencia si después querés <strong>pintar</strong>.</li>
          <li>Proceso rápido y prolijo: cuidamos bordes y venecitas.</li>
        </ul>

        <p class="mt-3 text-neutral-700">
          Luego, mantenemos el resultado con <strong
            >mantenimiento semanal o quincenal</strong
          >.
        </p>
      </div>

      <!-- Seguridad -->
      <div class="bg-white shadow-sm p-6 border border-neutral-200 rounded-2xl">
        <div class="inline-flex items-center gap-2 mb-3">
          <ShieldCheckIcon class="w-5 h-5 text-sky-600" />
          <h2 class="font-semibold text-neutral-900 text-xl">Seguridad</h2>
        </div>
        <ul class="space-y-2 mt-4 pl-6 text-neutral-700 list-disc">
          <li>EPP completo: guantes, máscaras, anteojos y botas.</li>
          <li>Área delimitada, ventilación y manipulación segura.</li>
          <li>Neutralizado final antes de retomar el uso de la pileta.</li>
        </ul>
      </div>
    </section>

    <!-- CTA -->
    <ServiceCTA serviceTitle={service.title} wspHref={wspHref} />
  </article>
</Layout>
