---
import Layout from "../../layouts/Layout.astro";
import { Image } from "astro:assets";
import asset from "@/assets/asset.png";
import {
  MoveLeft,
  CheckCircle2,
  ShieldCheck,
  Sparkles,
  Wrench,
} from "@lucide/astro";

const service = {
  title: "Implementación / adaptación de doble filtrado",
  desc: "Añadimos un segundo filtro (en paralelo o en serie) para aumentar caudal, claridad y redundancia. Incluye adecuación de cañerías y pruebas.",
  src: asset,
  alt: "Adaptación de doble filtrado en sala de máquinas",
  slug: "doble-filtrado",
};

const WSP = "5492644822296";
const wspHref = `https://wa.me/${WSP}?text=${encodeURIComponent(
  `Hola! Quiero solicitar el servicio de ${service.title}`
)}`;
---

<Layout title={service.title} description={service.desc} image={service.src}>
  <article class="space-y-10 mx-auto px-6 py-12 container">
    <!-- Top bar -->
    <nav class="flex justify-start items-center mb-6">
      <a
        href="javascript:history.back()"
        aria-label="Volver"
        class="inline-flex items-center gap-2 bg-neutral-100 hover:bg-neutral-200 px-4 py-2 rounded-full font-medium text-neutral-800 transition-colors"
      >
        <MoveLeft class="w-5 h-5" />
        <span>Volver</span>
      </a>
    </nav>

    <!-- Header -->
    <header class="space-y-4">
      <h1
        class="font-bold text-neutral-900 text-3xl md:text-4xl tracking-tight"
      >
        {service.title}
      </h1>
      <p class="text-neutral-700 text-lg">{service.desc}</p>
    </header>

    <!-- Cover -->
    <figure
      class="relative shadow rounded-3xl ring-1 ring-neutral-200/60 overflow-hidden"
    >
      <div class="relative h-64 md:h-[28rem]">
        <Image
          src={service.src}
          alt={service.alt}
          width={1920}
          height={1080}
          loading="eager"
          decoding="async"
          class="absolute inset-0 w-full h-full object-center object-cover"
        />
        <div
          class="absolute inset-0 bg-gradient-to-t from-black/40 via-black/10 to-transparent"
          aria-hidden="true"
        >
        </div>
        <div
          class="absolute -inset-10 bg-[radial-gradient(50%_60%_at_50%_100%,rgba(255,255,255,0.25),transparent_70%)] pointer-events-none"
          aria-hidden="true"
        >
        </div>
      </div>
      <figcaption
        class="bottom-0 absolute inset-x-0 flex justify-between items-center gap-4 px-4 md:px-6 py-3 text-white"
      >
        <span class="opacity-90 text-sm/5">{service.alt}</span>
      </figcaption>
    </figure>

    <!-- Cards -->
    <section class="gap-6 grid grid-cols-1 md:grid-cols-3">
      <!-- ¿Qué incluye? (corto) -->
      <div class="bg-white shadow-sm p-6 border border-neutral-200 rounded-2xl">
        <div class="inline-flex items-center gap-2 mb-3">
          <CheckCircle2 class="w-5 h-5 text-emerald-600" />
          <h2 class="font-semibold text-neutral-900 text-xl">¿Qué incluye?</h2>
        </div>
        <ul class="space-y-2 mt-4 pl-6 text-neutral-700 list-disc">
          <li>
            Diagnóstico hidráulico y esquema: paralelo o serie según tu sala.
          </li>
          <li>
            Adaptación de cañerías, válvula selectora/bypass y uniones rápidas.
          </li>
          <li>
            Pruebas de presión, fugas y caudal; puesta en marcha y entrega.
          </li>
        </ul>
      </div>

      <!-- Beneficios -->
      <div class="bg-white shadow-sm p-6 border border-neutral-200 rounded-2xl">
        <div class="inline-flex items-center gap-2 mb-3">
          <Sparkles class="w-5 h-5 text-indigo-600" />
          <h2 class="font-semibold text-neutral-900 text-xl">Beneficios</h2>
        </div>
        <ul class="space-y-2 mt-4 pl-6 text-neutral-700 list-disc">
          <li>Mayor caudal efectivo y agua más clara en menos tiempo.</li>
          <li>
            Backwash más eficiente y redundancia ante fallas de un filtro.
          </li>
        </ul>
        <p class="mt-3 text-neutral-700">
          Ideal complementar con mantenimiento semanal/quincenal.
        </p>
      </div>

      <!-- Seguridad -->
      <div class="bg-white shadow-sm p-6 border border-neutral-200 rounded-2xl">
        <div class="inline-flex items-center gap-2 mb-3">
          <ShieldCheck class="w-5 h-5 text-sky-600" />
          <h2 class="font-semibold text-neutral-900 text-xl">Seguridad</h2>
        </div>
        <ul class="space-y-2 mt-4 pl-6 text-neutral-700 list-disc">
          <li>
            Fijación de equipos, purga de aire y check-valves donde haga falta.
          </li>
          <li>
            Orden eléctrico y protección de cables/enchufes en sala húmeda.
          </li>
        </ul>
      </div>

      <!-- Opcionales / upgrades -->
      <div
        class="md:col-span-3 bg-white shadow-sm p-6 border border-neutral-200 rounded-2xl"
      >
        <div class="inline-flex items-center gap-2 mb-3">
          <Wrench class="w-5 h-5 text-indigo-600" />
          <h2 class="font-semibold text-neutral-900 text-xl">Opcionales</h2>
        </div>
        <ul
          class="gap-2 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 text-neutral-700"
        >
          <li class="flex items-center gap-2">
            <span class="bg-neutral-300 rounded-full w-1.5 h-1.5"></span>Medios
            filtrantes: arena, zeolita o vidrio.
          </li>
          <li class="flex items-center gap-2">
            <span class="bg-neutral-300 rounded-full w-1.5 h-1.5"
            ></span>Manómetro y caudalímetro para control.
          </li>
          <li class="flex items-center gap-2">
            <span class="bg-neutral-300 rounded-full w-1.5 h-1.5"></span>Bypass
            y válvula selectora adicional.
          </li>
          <li class="flex items-center gap-2">
            <span class="bg-neutral-300 rounded-full w-1.5 h-1.5"
            ></span>Adecuación de diámetros para menor pérdida de carga.
          </li>
        </ul>
      </div>
    </section>

    <!-- CTA -->
    <section>
      <div
        class="flex md:flex-row flex-col justify-between items-start md:items-center gap-5 bg-gradient-to-r from-blue-700 to-blue-500 shadow p-6 md:p-8 rounded-3xl text-white"
      >
        <div>
          <p class="font-semibold text-lg">¿Querés este servicio?</p>
          <p class="text-blue-50">
            Escribinos por WhatsApp y coordinamos la visita.
          </p>
        </div>

        <div class="flex flex-wrap gap-3">
          <a
            href={wspHref}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex justify-center items-center bg-white hover:bg-blue-50 px-5 py-3 rounded-full font-semibold text-blue-700 transition"
          >
            Solicitar por WhatsApp
          </a>
          <a
            href={`mailto:masterpiscinas.sj@gmail.com?subject=${encodeURIComponent(
              "Consulta: " + service.title
            )}`}
            class="inline-flex justify-center items-center hover:bg-white/10 px-5 py-3 border border-white/80 rounded-full text-white transition"
          >
            Enviar correo
          </a>
        </div>
      </div>
    </section>
  </article>
</Layout>
