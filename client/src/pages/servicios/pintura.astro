---
import Layout from "../../layouts/Layout.astro";
import { Image } from "astro:assets";
import asset from "@/assets/asset.png";
import pinturaAntes from "@/assets/pinturaAntes.webp";
import pinturaDespues from "@/assets/pinturaDespues.webp";
import {
  MoveLeft,
  ShieldCheck,
  Sparkles,
  Calendar,
  Thermometer,
  CircleCheckIcon,
} from "@lucide/astro";
import ServiceCTA from "@/components/ServiceCTA.astro";
import CarouselGallery from "@/components/CarouselGallery";

const service = {
  title: "Pintura",
  desc: "Tu pileta renovada de punta a punta: preparamos la superficie y aplicamos pintura premium para un acabado parejo y duradero.",
  src: asset,
  alt: "Pintura de pileta",
  slug: "pintura",
};

const WSP = "5492644822296";
const wspHref = `https://wa.me/${WSP}?text=${encodeURIComponent(
  `Hola! Quiero solicitar el servicio de ${service.title}`
)}`;

const slides = [
  { src: pinturaAntes, alt: "Pintura de pileta (antes)" },
  { src: pinturaDespues, alt: "Pintura de pileta (después)" },
];
---

<Layout title={service.title} description={service.desc}>
  <article class="space-y-10 mx-auto px-6 py-12 container">
    <!-- Top bar -->
    <nav class="flex justify-start items-center mb-6">
      <a
        href="/"
        aria-label="Volver al inicio"
        class="inline-flex items-center gap-2 bg-neutral-100 hover:bg-neutral-200 px-4 py-2 rounded-full font-medium text-neutral-800 transition-colors"
      >
        <MoveLeft class="w-5 h-5" />
        <span>Inicio</span>
      </a>
    </nav>

    <!-- Header -->
    <header class="space-y-3">
      <h1
        class="font-bold text-neutral-900 text-3xl md:text-4xl tracking-tight"
      >
        {service.title}
      </h1>
      <p class="text-neutral-700 text-lg">{service.desc}</p>

      <!-- Temporada / Temperatura -->
      <div
        class="md:flex md:justify-between md:items-center gap-4 bg-white p-4 border border-neutral-200 rounded-2xl"
      >
        <p class="inline-flex items-center gap-2 text-neutral-700">
          <Calendar class="w-4 h-4 text-neutral-600" aria-hidden="true" />
          <span
            ><strong>Mejor temporada:</strong> abril → octubre (agendamos desde mayo
            según clima).</span
          >
        </p>
        <p class="inline-flex items-center gap-2 mt-2 md:mt-0 text-neutral-600">
          <Thermometer class="w-4 h-4 text-neutral-600" aria-hidden="true" />
          <span
            ><strong>Aplicación ideal:</strong> 5&nbsp;°C a 25&nbsp;°C, sin sol directo
            ni viento fuerte.</span
          >
        </p>
      </div>
    </header>

    <!-- Cover -->
    <section class="relative">
      <CarouselGallery slides={slides} client:load />
    </section>

    <!-- Cards -->
    <section class="gap-6 grid grid-cols-1 md:grid-cols-3">
      <!-- ¿Qué incluye? -->
      <div class="bg-white shadow-sm p-6 border border-neutral-200 rounded-2xl">
        <div class="inline-flex items-center gap-2 mb-3">
          <CircleCheckIcon class="w-5 h-5 text-emerald-600" />
          <h2 class="font-semibold text-neutral-900 text-xl">¿Qué hacemos?</h2>
        </div>
        <ul class="space-y-2 mt-4 pl-6 text-neutral-700 list-disc">
          <li>Desagote y limpieza profunda de toda la superficie.</li>
          <li>Lijado exhaustivo y retiro de pintura floja.</li>
          <li>Triple lavado: detergente → cloro → ácido.</li>
          <li>Secado 48–72&nbsp;hs y aplicación de pintura premium.</li>
          <li>Con o sin materiales, según prefieras.</li>
        </ul>
        <p class="mt-3 text-neutral-600 text-sm">
          Elegimos <em>tipo y color</em> con vos (según base existente y uso).
        </p>
      </div>

      <!-- Beneficios -->
      <div class="bg-white shadow-sm p-6 border border-neutral-200 rounded-2xl">
        <div class="inline-flex items-center gap-2 mb-3">
          <Sparkles class="w-5 h-5 text-indigo-600" />
          <h2 class="font-semibold text-neutral-900 text-xl">Beneficios</h2>
        </div>
        <ul class="space-y-2 mt-4 pl-6 text-neutral-700 list-disc">
          <li>Acabado parejo y colores que realzan el agua.</li>
          <li>Mejor sensación al tacto y fácil limpieza.</li>
          <li>Protección de la superficie frente al sol y químicos.</li>
        </ul>
        <p class="mt-3 text-neutral-700">
          ¿La querés perfecta? Sumá <strong
            >limpieza con ácido + hidrolavado</strong
          > previo.
        </p>
      </div>

      <!-- Cómo proteger la pintura -->
      <div class="bg-white shadow-sm p-6 border border-neutral-200 rounded-2xl">
        <div class="inline-flex items-center gap-2 mb-3">
          <ShieldCheck class="w-5 h-5 text-sky-600" />
          <h2 class="font-semibold text-neutral-900 text-xl">Cómo dura más</h2>
        </div>
        <ul class="space-y-2 mt-4 pl-6 text-neutral-700 list-disc">
          <li>Usá cobertor cuando no se utilice (sol sanjuanino intenso).</li>
          <li>Filtrado según plan y retrolavado cuando sube la presión.</li>
          <li>Dosificación correcta de químicos (sin excesos).</li>
        </ul>
        <p class="mt-3 text-neutral-700">
          Te dejamos <strong>instrucciones post-pintura</strong> claras y simples.
        </p>
      </div>
    </section>

    <!-- CTA -->
    <ServiceCTA
      serviceTitle={service.title}
      wspHref={wspHref}
      title="¿Listo para renovar tu pileta?"
      subtitle="Coordinamos visita, te asesoramos en colores y dejamos todo preparado para que luzca impecable."
    />
  </article>
</Layout>
