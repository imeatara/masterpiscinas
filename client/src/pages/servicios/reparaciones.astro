---
import Layout from "../../layouts/Layout.astro";
import { Image } from "astro:assets";
import asset from "@/assets/asset.png";
import { MoveLeft, CheckCircle2, ShieldCheck, Sparkles } from "@lucide/astro";

const service = {
  title: "Reparaciones (fibra, cemento, caños pinchados)",
  desc: "Parcheo y refuerzos: en fibra usamos resina + fibra y roving en zonas críticas; en cemento, selladores de grietas y, si hace falta, anclajes con hierro. También reparamos caños pinchados.",
  src: asset,
  alt: "Reparaciones de piletas (fibra, cemento y caños)",
  slug: "reparaciones",
};

const WSP = "5492644822296";
const wspHref = `https://wa.me/${WSP}?text=${encodeURIComponent(
  `Hola! Quiero solicitar el servicio de ${service.title}`
)}`;
---

<Layout title={service.title} description={service.desc} image={service.src}>
  <article class="mx-auto container px-6 py-12 space-y-10">
    <!-- Top bar -->
    <nav class="flex items-center justify-between">
      <a
        href="javascript:history.back()"
        aria-label="Volver"
        class="inline-flex items-center justify-center rounded-full p-2 hover:bg-slate-100 transition"
      >
        <MoveLeft class="w-5 h-5" />
      </a>

      <ol class="hidden md:flex items-center gap-2 text-sm text-slate-500">
        <li>
          <a href="/" class="hover:text-slate-800 hover:underline">Inicio</a>
        </li>
        <li aria-hidden="true">/</li>
        <li class="text-slate-700 font-medium" aria-current="page">
          {service.title}
        </li>
      </ol>
    </nav>

    <!-- Header -->
    <header class="space-y-4">
      <h1 class="text-3xl md:text-4xl font-bold tracking-tight text-slate-900">
        {service.title}
      </h1>
      <p class="text-lg text-slate-700">{service.desc}</p>
    </header>

    <!-- Cover -->
    <figure
      class="relative overflow-hidden rounded-3xl ring-1 ring-slate-200/60 shadow"
    >
      <div class="relative h-64 md:h-[28rem]">
        <Image
          src={service.src}
          alt={service.alt}
          width={1920}
          height={1080}
          loading="eager"
          decoding="async"
          class="absolute inset-0 h-full w-full object-cover object-center"
        />
        <div
          class="absolute inset-0 bg-gradient-to-t from-black/40 via-black/10 to-transparent"
          aria-hidden="true"
        >
        </div>
        <div
          class="pointer-events-none absolute -inset-10 bg-[radial-gradient(50%_60%_at_50%_100%,rgba(255,255,255,0.25),transparent_70%)]"
          aria-hidden="true"
        >
        </div>
      </div>
      <figcaption
        class="absolute inset-x-0 bottom-0 flex items-center justify-between gap-4 px-4 md:px-6 py-3 text-white"
      >
        <span class="text-sm/5 opacity-90">{service.alt}</span>
      </figcaption>
    </figure>

    <!-- Cards -->
    <section class="grid grid-cols-1 md:grid-cols-3 gap-6">
      <!-- ¿Qué incluye? -->
      <!-- ¿Qué incluye? (versión corta) -->
      <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
        <div class="mb-3 inline-flex items-center gap-2">
          <CheckCircle2 class="w-5 h-5 text-emerald-600" />
          <h2 class="text-xl font-semibold text-slate-900">¿Qué incluye?</h2>
        </div>
        <ul class="mt-3 space-y-2 text-slate-700">
          <li class="flex gap-2">
            <span class="mt-1.5 h-1.5 w-1.5 rounded-full bg-slate-300"></span>
            <strong>Fibra:</strong> resina + fibra; refuerzo con roving en zonas
            críticas.
          </li>
          <li class="flex gap-2">
            <span class="mt-1.5 h-1.5 w-1.5 rounded-full bg-slate-300"></span>
            <strong>Cemento:</strong> selladores según fisura; si hace falta, anclajes
            con hierro.
          </li>
          <li class="flex gap-2">
            <span class="mt-1.5 h-1.5 w-1.5 rounded-full bg-slate-300"></span>
            <strong>Caños pinchados:</strong> detección y reparación del tramo afectado.
          </li>
        </ul>
      </div>

      <!-- Seguridad -->
      <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
        <div class="mb-3 inline-flex items-center gap-2">
          <ShieldCheck class="w-5 h-5 text-sky-600" />
          <h2 class="text-xl font-semibold text-slate-900">Seguridad</h2>
        </div>
        <ul class="mt-3 space-y-2 text-slate-700">
          <li class="flex gap-2">
            <span class="mt-1.5 h-1.5 w-1.5 rounded-full bg-slate-300"
            ></span>EPP: guantes, máscaras, anteojos y botas.
          </li>
          <li class="flex gap-2">
            <span class="mt-1.5 h-1.5 w-1.5 rounded-full bg-slate-300"
            ></span>Área delimitada y ventilación adecuada.
          </li>
        </ul>
      </div>

      <!-- Recomendaciones -->
      <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
        <div class="mb-3 inline-flex items-center gap-2">
          <Sparkles class="w-5 h-5 text-indigo-600" />
          <h2 class="text-xl font-semibold text-slate-900">Recomendaciones</h2>
        </div>
        <ul class="mt-3 space-y-2 text-slate-700">
          <li class="flex gap-2">
            <span class="mt-1.5 h-1.5 w-1.5 rounded-full bg-slate-300"></span>No
            excederse con la cantidad de personas al mismo tiempo.
          </li>
          <li class="flex gap-2">
            <span class="mt-1.5 h-1.5 w-1.5 rounded-full bg-slate-300"
            ></span>Evitar golpes y usos bruscos.
          </li>
          <li class="flex gap-2">
            <span class="mt-1.5 h-1.5 w-1.5 rounded-full bg-slate-300"></span>No
            dejar la pileta <strong>vacía</strong> al solazo.
          </li>
        </ul>
        <p class="mt-3 text-sm text-slate-500">
          Nota: las grietas suelen generarse por golpes o por exceso de peso
          (mucha gente dentro de la pileta).
        </p>
      </div>
    </section>

    <!-- CTA -->
    <section>
      <div
        class="flex flex-col md:flex-row items-start md:items-center justify-between gap-5 rounded-3xl bg-gradient-to-r from-blue-700 to-blue-500 p-6 md:p-8 text-white shadow"
      >
        <div>
          <p class="text-lg font-semibold">¿Querés este servicio?</p>
          <p class="text-blue-50">
            Escribinos por WhatsApp y coordinamos una visita técnica.
          </p>
        </div>

        <div class="flex flex-wrap gap-3">
          <a
            href={wspHref}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center justify-center rounded-full bg-white px-5 py-3 font-semibold text-blue-700 transition hover:bg-blue-50"
          >
            Solicitar por WhatsApp
          </a>
          <a
            href={`mailto:masterpiscinas.sj@gmail.com?subject=${encodeURIComponent("Consulta: " + service.title)}`}
            class="inline-flex items-center justify-center rounded-full border border-white/80 px-5 py-3 text-white transition hover:bg-white/10"
          >
            Enviar correo
          </a>
        </div>
      </div>
    </section>
  </article>
</Layout>
