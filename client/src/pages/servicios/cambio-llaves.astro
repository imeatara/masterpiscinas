---
import Layout from "../../layouts/Layout.astro";
import { Image } from "astro:assets";
import asset from "@/assets/asset.png";
import cambioLlavesAntes from "@/assets/cambioLlavesAntes.webp";
import cambioLlavesDespues from "@/assets/cambioLlavesDespues.webp";
import { MoveLeft, CheckCircle2, ShieldCheck, Wrench } from "@lucide/astro";
import ServiceCTA from "@/components/ServiceCTA.astro";
import CarouselGallery from "@/components/CarouselGallery";

const service = {
  title: "Cambio de llaves",
  desc: "Más control, mejor filtrado y agua clara: renovamos las llaves y separamos skimmer, fondo y barrefondo.",
  src: asset,
  alt: "Cambio de llaves en sala de máquinas",
  slug: "cambio-de-llaves",
};

const WSP = "5492644822296";
const wspHref = `https://wa.me/${WSP}?text=${encodeURIComponent(
  `Hola! Quiero solicitar el servicio de ${service.title}`
)}`;

const slides = [
  { src: cambioLlavesAntes, alt: "Cambio de llaves (antes)" },
  { src: cambioLlavesDespues, alt: "Cambio de llaves (después)" },
];
---

<Layout title={service.title} description={service.desc} image={service.src}>
  <article class="space-y-10 mx-auto px-6 py-12 container">
    <!-- Volver -->
    <nav class="flex justify-start items-center mb-6">
      <a
        href="/"
        aria-label="Volver al inicio"
        class="inline-flex items-center gap-2 bg-neutral-100 hover:bg-neutral-200 px-4 py-2 rounded-full font-medium text-neutral-800 transition-colors"
      >
        <MoveLeft class="w-5 h-5" />
        <span>Inicio</span>
      </a>
    </nav>

    <!-- Encabezado -->
    <header class="space-y-2">
      <h1
        class="font-bold text-neutral-900 text-3xl md:text-4xl tracking-tight"
      >
        {service.title}
      </h1>
      <p class="text-neutral-700 text-lg">{service.desc}</p>
    </header>

    <!-- Cover -->
    <section class="relative">
      <CarouselGallery slides={slides} client:load />
    </section>

    <!-- Contenido -->
    <section class="gap-6 grid grid-cols-1 md:grid-cols-3">
      <!-- Qué hacemos -->
      <div class="bg-white shadow-sm p-6 border border-neutral-200 rounded-2xl">
        <div class="inline-flex items-center gap-2 mb-3">
          <CheckCircle2 class="w-5 h-5 text-emerald-600" />
          <h2 class="font-semibold text-neutral-900 text-xl">¿Qué hacemos?</h2>
        </div>
        <ul class="space-y-2 mt-3 text-neutral-700">
          <li class="flex items-start gap-3">
            <span class="bg-neutral-300 mt-2 rounded-full w-2 h-2 shrink-0"
            ></span>
            <span
              >Reemplazo de llaves viejas o con fugas por llaves nuevas.</span
            >
          </li>
          <li class="flex items-start gap-3">
            <span class="bg-neutral-300 mt-2 rounded-full w-2 h-2 shrink-0"
            ></span>
            <span
              >Independizamos <strong>skimmer, fondo y barrefondo</strong> para control
              individual.</span
            >
          </li>
          <li class="flex items-start gap-3">
            <span class="bg-neutral-300 mt-2 rounded-full w-2 h-2 shrink-0"
            ></span>
            <span>Prueba de estanqueidad y rotulado para uso sencillo.</span>
          </li>
        </ul>
      </div>

      <!-- Beneficios -->
      <div class="bg-white shadow-sm p-6 border border-neutral-200 rounded-2xl">
        <div class="inline-flex items-center gap-2 mb-3">
          <Wrench class="w-5 h-5 text-indigo-600" />
          <h2 class="font-semibold text-neutral-900 text-xl">Beneficios</h2>
        </div>
        <ul class="space-y-2 mt-3 text-neutral-700">
          <li class="flex items-start gap-3">
            <span class="bg-neutral-300 mt-2 rounded-full w-2 h-2 shrink-0"
            ></span>
            <span
              ><strong>Mejor circulación:</strong> succión desde varios puntos, agua
              más clara.</span
            >
          </li>
          <li class="flex items-start gap-3">
            <span class="bg-neutral-300 mt-2 rounded-full w-2 h-2 shrink-0"
            ></span>
            <span
              ><strong>Más control:</strong> elegís si toma del fondo, barrefondo
              o skimmer.</span
            >
          </li>
          <li class="flex items-start gap-3">
            <span class="bg-neutral-300 mt-2 rounded-full w-2 h-2 shrink-0"
            ></span>
            <span
              ><strong>Filtrado parejo</strong> y menos esfuerzo de la bomba.</span
            >
          </li>
        </ul>
        <p class="mt-3 text-neutral-700">
          ¿Tu pileta no tiene fondo o barrefondo? Adaptamos la configuración
          para aprovechar al máximo el sistema.
        </p>
      </div>

      <!-- Seguridad -->
      <div class="bg-white shadow-sm p-6 border border-neutral-200 rounded-2xl">
        <div class="inline-flex items-center gap-2 mb-3">
          <ShieldCheck class="w-5 h-5 text-sky-600" />
          <h2 class="font-semibold text-neutral-900 text-xl">Seguridad</h2>
        </div>
        <ul class="space-y-2 mt-3 text-neutral-700">
          <li class="flex items-start gap-3">
            <span class="bg-neutral-300 mt-2 rounded-full w-2 h-2 shrink-0"
            ></span>
            <span>Materiales aptos para piscina y pegados/roscas seguros.</span>
          </li>
          <li class="flex items-start gap-3">
            <span class="bg-neutral-300 mt-2 rounded-full w-2 h-2 shrink-0"
            ></span>
            <span
              >Corte eléctrico/hidráulico y verificación antes de poner en
              marcha.</span
            >
          </li>
        </ul>
      </div>
    </section>

    <!-- CTA -->
    <ServiceCTA
      serviceTitle={service.title}
      wspHref={wspHref}
      title="¿Querés más claridad y mejor rendimiento?"
      subtitle="Independizamos tus llaves y dejamos la sala de máquinas lista para un filtrado de 10."
    />
  </article>
</Layout>
