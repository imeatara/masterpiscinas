---
import Layout from "../../layouts/Layout.astro";
import { Image } from "astro:assets";
import asset from "@/assets/asset.png";
import { MoveLeft, CheckCircle2, ShieldCheck, Sparkles } from "@lucide/astro";
import ServiceCTA from "@/components/ServiceCTA.astro";

const service = {
  title: "Desagote de piletas",
  desc: "Vaciado seguro y controlado con bombas propias. También desagotamos sótanos, cisternas y viviendas por inundaciones.",
  src: asset,
  alt: "Desagote de pileta con bomba sumergible",
  slug: "desagote",
};

const WSP = "5492644822296";
const wspHref = `https://wa.me/${WSP}?text=${encodeURIComponent(
  `Hola! Quiero solicitar el servicio de ${service.title}`
)}`;
---

<Layout title={service.title} description={service.desc} image={service.src}>
  <article class="space-y-10 mx-auto px-6 py-12 container">
    <!-- Top bar -->
    <nav class="flex justify-start items-center mb-6">
      <a
        href="/"
        aria-label="Volver al inicio"
        class="inline-flex items-center gap-2 bg-neutral-100 hover:bg-neutral-200 px-4 py-2 rounded-full font-medium text-neutral-800 transition-colors"
      >
        <MoveLeft class="w-5 h-5" />
        <span>Inicio</span>
      </a>
    </nav>

    <!-- Header -->
    <header class="space-y-4">
      <h1
        class="font-bold text-neutral-900 text-3xl md:text-4xl tracking-tight"
      >
        {service.title}
      </h1>
      <p class="text-neutral-700 text-lg">{service.desc}</p>
    </header>

    <!-- Cover -->
    <figure
      class="relative shadow rounded-3xl ring-1 ring-neutral-200/60 overflow-hidden"
    >
      <div class="relative h-64 md:h-[28rem]">
        <Image
          src={service.src}
          alt={service.alt}
          width={1920}
          height={1080}
          loading="eager"
          decoding="async"
          class="absolute inset-0 w-full h-full object-center object-cover"
        />
        <div
          class="absolute inset-0 bg-gradient-to-t from-black/40 via-black/10 to-transparent"
          aria-hidden="true"
        >
        </div>
        <div
          class="absolute -inset-10 bg-[radial-gradient(50%_60%_at_50%_100%,rgba(255,255,255,0.25),transparent_70%)] pointer-events-none"
          aria-hidden="true"
        >
        </div>
      </div>
      <figcaption
        class="bottom-0 absolute inset-x-0 flex justify-between items-center gap-4 px-4 md:px-6 py-3 text-white"
      >
        <span class="opacity-90 text-sm/5">{service.alt}</span>
      </figcaption>
    </figure>

    <!-- Cards -->
    <section class="gap-6 grid grid-cols-1 md:grid-cols-3">
      <!-- ¿Qué incluye? (corto) -->
      <div class="bg-white shadow-sm p-6 border border-neutral-200 rounded-2xl">
        <div class="inline-flex items-center gap-2 mb-3">
          <CheckCircle2 class="w-5 h-5 text-emerald-600" />
          <h2 class="font-semibold text-neutral-900 text-xl">¿Qué incluye?</h2>
        </div>
        <ul class="space-y-2 mt-3 text-neutral-700">
          <li class="flex gap-2">
            <span class="bg-neutral-300 mt-1.5 rounded-full w-1.5 h-1.5"></span>
            Bombas de desagote y mangueras propias. No usamos equipos del hogar.
          </li>
          <li class="flex gap-2">
            <span class="bg-neutral-300 mt-1.5 rounded-full w-1.5 h-1.5"></span>
            Vaciado controlado y monitoreo de nivel para proteger la estructura.
          </li>
          <li class="flex gap-2">
            <span class="bg-neutral-300 mt-1.5 rounded-full w-1.5 h-1.5"></span>
            Descarga dirigida al punto indicado por el cliente.
          </li>
        </ul>
      </div>

      <!-- Beneficios -->
      <div class="bg-white shadow-sm p-6 border border-neutral-200 rounded-2xl">
        <div class="inline-flex items-center gap-2 mb-3">
          <Sparkles class="w-5 h-5 text-indigo-600" />
          <h2 class="font-semibold text-neutral-900 text-xl">Beneficios</h2>
        </div>
        <ul class="space-y-2 mt-3 text-neutral-700">
          <li class="flex gap-2">
            <span class="bg-neutral-300 mt-1.5 rounded-full w-1.5 h-1.5"></span>
            Base lista para limpieza profunda, reparación o pintura.
          </li>
          <li class="flex gap-2">
            <span class="bg-neutral-300 mt-1.5 rounded-full w-1.5 h-1.5"></span>
            Proceso rápido y prolijo, cuidando bordes y solados.
          </li>
        </ul>
        <p class="mt-3 text-neutral-700">
          Al finalizar, ideal complementar con <strong
            >limpieza con ácido</strong
          > o
          <strong>mantenimiento</strong> semanal/quincenal.
        </p>
      </div>

      <!-- Seguridad -->
      <div class="bg-white shadow-sm p-6 border border-neutral-200 rounded-2xl">
        <div class="inline-flex items-center gap-2 mb-3">
          <ShieldCheck class="w-5 h-5 text-sky-600" />
          <h2 class="font-semibold text-neutral-900 text-xl">Seguridad</h2>
        </div>
        <ul class="space-y-2 mt-3 text-neutral-700">
          <li class="flex gap-2">
            <span class="bg-neutral-300 mt-1.5 rounded-full w-1.5 h-1.5"></span>
            EPP: guantes, máscaras, anteojos y botas.
          </li>
          <li class="flex gap-2">
            <span class="bg-neutral-300 mt-1.5 rounded-full w-1.5 h-1.5"></span>
            Área delimitada, cables protegidos y corte eléctrico si corresponde.
          </li>
        </ul>
      </div>

      <!-- También desagotamos -->
      <div
        class="md:col-span-3 bg-white shadow-sm p-6 border border-neutral-200 rounded-2xl"
      >
        <div class="inline-flex items-center gap-2 mb-3">
          <CheckCircle2 class="w-5 h-5 text-emerald-600" />
          <h2 class="font-semibold text-neutral-900 text-xl">
            También desagotamos
          </h2>
        </div>
        <ul
          class="gap-2 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 text-neutral-700"
        >
          <li class="flex items-center gap-2">
            <span class="bg-neutral-300 rounded-full w-1.5 h-1.5"></span>Piletas
          </li>
          <li class="flex items-center gap-2">
            <span class="bg-neutral-300 rounded-full w-1.5 h-1.5"></span>Sótanos
          </li>
          <li class="flex items-center gap-2">
            <span class="bg-neutral-300 rounded-full w-1.5 h-1.5"
            ></span>Cisternas
          </li>
          <li class="flex items-center gap-2">
            <span class="bg-neutral-300 rounded-full w-1.5 h-1.5"></span>Casas
            por inundaciones
          </li>
        </ul>
      </div>
    </section>

    <!-- CTA -->
    <ServiceCTA />
  </article>
</Layout>
